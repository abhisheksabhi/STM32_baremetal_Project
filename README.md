# STM32F4 Bare-Metal Round-Robin Task Scheduler

**Target:** STM32F407 (Cortex-M4, HSI 16 MHz)

## Overview

This project implements a cooperative/preemptive round-robin task scheduler from scratch on the STM32F4 series, without any RTOS or HAL library. It demonstrates bare-metal startup, a custom linker script, SysTick-driven context switching via PendSV, and GPIO-based LED control.

Four user tasks blink four on-board LEDs (STM32F4-Discovery: PD12–PD15) at different rates. An idle task runs when all user tasks are blocked.

---

## Project Structure

| File | Description |
|------|-------------|
| `main.c` | Scheduler core, task definitions, SysTick/PendSV handlers |
| `main.h` | Stack layout macros, task state definitions, tick config |
| `led.c` | GPIO init and LED on/off drivers (GPIOD, RCC AHB1) |
| `led.h` | LED pin definitions (12=GREEN, 13=ORANGE, 14=RED, 15=BLUE) |
| `stm32_startupByMe.c` | Custom startup: vector table, Reset_Handler (data/bss init) |
| `stm32_Myls.ld` | Custom linker script: FLASH/SRAM layout and linker symbols |
| `syscalls.c` | Newlib syscall stubs + semihosting support (`_sbrk`, `_write`, etc.) |
| `Makefile` | Build rules for `arm-none-eabi-gcc` |
| `final.map` | Linker map output (generated by `make`) |
| `STARTUP_LINKER_README.md` | Detailed notes on the startup code and linker script |

---

## Memory Layout

| Region | Address | Size | Usage |
|--------|---------|------|-------|
| FLASH | `0x08000000` | 1024K | Code, vector table, `.rodata`, `.data` init values |
| SRAM | `0x20000000` | 128K | `.data`, `.bss`, heap, task stacks, scheduler stack |

### Stack Allocation (top of SRAM downward, 1024 bytes each)

| Stack | Address |
|-------|---------|
| `T1_STACK_START` | `SRAM_END` = `0x20020000` |
| `T2_STACK_START` | `SRAM_END - 1K` |
| `T3_STACK_START` | `SRAM_END - 2K` |
| `T4_STACK_START` | `SRAM_END - 3K` |
| `IDLE_STACK_START` | `SRAM_END - 4K` |
| `SCHED_STACK_START` | `SRAM_END - 5K` (MSP, used during boot and ISRs) |

---

## Linker Symbols (`stm32_Myls.ld`)

| Symbol | Description |
|--------|-------------|
| `_etext` | End of `.text` section in FLASH |
| `_loadaddrss_data` | `LOADADDR(.data)` — where `.data` initializers live in FLASH |
| `_sdata` / `_edata` | `.data` section bounds in SRAM |
| `_sbss` / `_ebss` | `.bss` section bounds in SRAM |
| `_estack` | Top of SRAM (`ORIGIN(SRAM) + LENGTH(SRAM)` = `0x20020000`) |
| `end` | End of `.bss` (heap start, used by `_sbrk` in `syscalls.c`) |

---

## Startup Sequence (`stm32_startupByMe.c`)

1. Vector table placed at `0x08000000` (`.isr_vector`) — first entry is `STACK_START`.
2. `Reset_Handler` runs on power-on/reset:
   - Copies `.data` initializers from FLASH (`_loadaddrss_data`) to SRAM (`_sdata`) — word-based copy (`uint32_t`), 4-byte alignment guaranteed by linker.
   - Zero-initializes `.bss` (`_sbss` to `_ebss`), word-based.
   - Calls `__libc_init_array()` for C runtime / C++ constructors.
   - Calls `main()`. If `main()` returns, loops forever.
3. All IRQ handlers are weakly aliased to `Default_Handler` (infinite loop).  
   Overridden in `main.c`: `HardFault_Handler`, `MemManage_Handler`, `BusFault_Handler`, `PendSV_Handler`, `SysTick_Handler`.

---

## Scheduler Design (`main.c`)

- `MAX_TASKS = 5` (1 idle + 4 user tasks).
- **TCB** (Task Control Block) holds: `psp_value`, `block_count`, `current_state`, `task_handler`.
- Task states: `TASK_READY_STATE` (`0x00`), `TASK_BLOCKED_STATE` (`0xFF`).

### Initialization sequence

1. `enable_processor_faults()` — enables MemManage, BusFault, UsageFault via `SCB->SHCSR`.
2. `initialise_monitor_handles()` — semihosting init (printf output via debugger).
3. `init_scheduler_stack()` — sets MSP to `SCHED_STACK_START` (naked function, `MSR MSP`).
4. `init_tasks_stack()` — fills each task's initial stack frame:
   - `xPSR = 0x01000000` (Thumb bit set)
   - `PC` = task handler address
   - `LR = 0xFFFFFFFD` (EXC_RETURN: return to Thread mode, use PSP)
   - `R0–R12 = 0`
5. `led_init_all()` — enables GPIOD clock, sets PD12–PD15 to output mode.
6. `init_systick_timer(1000)` — 1 ms tick at 16 MHz HSI (count = 15999).
7. `switch_sp_to_psp()` — loads PSP with task1's stack, sets `CONTROL.SPSEL = 1`.
8. Enters `task1_handler()` directly.

### Context switching (`PendSV_Handler`, naked)

- **Save:** `MRS R0,PSP` → `STMDB R0!,{R4-R11}` → save R0 to current TCB.
- `update_next_task()`: round-robin; skips `TASK_BLOCKED_STATE` tasks; falls back to idle.
- **Restore:** load next TCB's PSP → `LDMIA R0!,{R4-R11}` → `MSR PSP,R0`.
- `BX LR` — EXC_RETURN `0xFFFFFFFD` restores `xPSR/PC/LR/R0–R3/R12` from stack.

### `SysTick_Handler` (1 ms)

- Increments `g_tick_count`.
- `unblock_tasks()`: marks tasks READY when `block_count == g_tick_count`.
- Pends PendSV for context switch.

### `task_delay(tick_count)`

Disables interrupts (PRIMASK), sets `block_count = g_tick_count + tick_count`, sets state to `TASK_BLOCKED_STATE`, pends PendSV, re-enables interrupts.

---

## Task Behavior

| Task | LED | Pin | On | Off |
|------|-----|-----|----|-----|
| Task 1 | GREEN | PD12 | 1000 ms | 1000 ms |
| Task 2 | ORANGE | PD13 | 1000 ms | 1000 ms |
| Task 3 | BLUE | PD15 | 250 ms | 250 ms |
| Task 4 | RED | PD14 | 125 ms | 125 ms |
| Idle | — | — | Busy loop when all tasks blocked | — |

---

## Build

### Requirements

#### 1. GNU Arm Embedded Toolchain — `arm-none-eabi-gcc`

| | |
|---|---|
| **Version used** | 14.2.Rel1 (arm-none-eabi-gcc 14.2.1 20241119) |
| **Download** | https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads |
| **Windows installer** | Under section **"arm-none-eabi"** → Windows hosted cross toolchains → `.exe` installer |
| **winget** | `winget install Arm.GnuArmEmbeddedToolchain` |
| **Default install path** | `C:\Program Files (x86)\Arm GNU Toolchain arm-none-eabi\14.2 rel1\bin` |

> Add the `bin` folder to your system `PATH` so `arm-none-eabi-gcc` is accessible from any terminal.

#### 2. GNU Make

| | |
|---|---|
| **Version used** | 3.81 (GnuWin32) |
| **Download** | https://gnuwin32.sourceforge.net/packages/make.htm → "Complete package, except sources" |
| **winget** | `winget install GnuWin32.Make` |
| **Default install path** | `C:\Program Files (x86)\GnuWin32\bin` |

> Add the `bin` folder to your system `PATH` so `make` is accessible from any terminal.

#### Verify installation

```bash
arm-none-eabi-gcc --version
# arm-none-eabi-gcc.exe (Arm GNU Toolchain 14.2.Rel1 ...) 14.2.1 20241119

make --version
# GNU Make 3.81
```

### Commands

```bash
make        # builds build/final.elf, build/*.o, build/*.s, build/final.map
make clean  # removes the entire build/ directory
```

### Flags

| Type | Flags |
|------|-------|
| Compiler | `-mcpu=cortex-m4 -mthumb -std=gnu11 -Wall -O0` |
| Assembly | `-S -mcpu=cortex-m4 -mthumb -std=gnu11 -Wall -O0` |
| Linker | `-mcpu=cortex-m4 -mthumb --specs=nano.specs --specs=nosys.specs -T stm32_Myls.ld -Wl,-Map=build/final.map` |

### Build output (`build/`)

| File | Description |
|------|-------------|
| `build/final.elf` | Linked ELF binary — flash this to the target |
| `build/final.map` | Linker map (symbol/section addresses) |
| `build/main.o` | Compiled object |
| `build/main.s` | Assembly listing of `main.c` |
| `build/led.o` / `build/led.s` | Compiled + assembly listing of `led.c` |
| `build/stm32_startupByMe.o` / `.s` | Compiled + assembly listing of startup |
| `build/syscalls.o` / `.s` | Compiled + assembly listing of syscalls |

Flash the resulting `build/final.elf` with OpenOCD, ST-Link Utility, or your preferred programmer targeting the STM32F407.

---

## Semihosting / printf

`syscalls.c` provides Newlib stubs. `initialise_monitor_handles()` is a no-op stub. To enable actual semihosting output via a debugger, link with `--specs=rdimon.specs` and implement `__io_putchar` / `__io_getchar`, or connect a UART and redirect `_write` there.

---

## Known Issues

> **Both issues below have been fixed in the current codebase.**

### ~~FPU not initialized~~ — Fixed

The FPU is now enabled in `Reset_Handler` (`stm32_startupByMe.c`) before `main()` is called, by setting CP10 and CP11 to full access in the CPACR register:

```c
*((volatile uint32_t*)0xE000ED88U) |= (0xFUL << 20);
```

The `#warning` guard in `main.c` no longer fires since `-mfloat-abi=soft` is the effective behavior with the current flags (no `-mfpu` specified).

### ~~INTERRUPT_DISABLE / INTERRUPT_ENABLE clobber R0~~ — Fixed

The macros in `main.h` have been replaced with `CPSID I` / `CPSIE I` — single atomic instructions that do not touch any general-purpose registers, with a `"memory"` barrier clobber to prevent compiler reordering:

```c
#define INTERRUPT_DISABLE()  do{ __asm volatile ("CPSID I" ::: "memory"); } while(0)
#define INTERRUPT_ENABLE()   do{ __asm volatile ("CPSIE I" ::: "memory"); } while(0)
```

---

## File History

| File | Note |
|------|------|
| `stm32_startup.c` | Earlier startup version, kept for reference |
| `stm32_startupByMe.c` | Current startup — word-based copy, `const` vector table, `noreturn` Reset_Handler, `_estack` from linker |
| `main_log` | Build/debug log (not source code) |
| `main.s` | Assembly listing output (not source code) |
